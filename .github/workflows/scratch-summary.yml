name: scratch-summary

on:
  push:
    branches:
      - main
    paths:
      - "scratch/*"
      - ".github/workflows/scratch-summary.yml"
  workflow_dispatch:

jobs:
  scratch-summary:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Get latest commit SHA
        id: commit
        run: echo "sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT
      - name: Generate SCRATCH.md
        run: |
          echo "# Scratch" > SCRATCH.md
          echo "" >> SCRATCH.md

          for file in $(ls -t scratch/* 2>/dev/null); do
            [ -f "$file" ] || continue
            name=$(basename "$file")
            echo "- [$name]($file)" >> SCRATCH.md
          done

          echo "" >> SCRATCH.md
          echo "---" >> SCRATCH.md
          echo "" >> SCRATCH.md
          echo "*This document was automatically generated based on commit [\`${{ steps.commit.outputs.sha }}\`](https://github.com/${{ github.repository }}/commit/${{ steps.commit.outputs.sha }}).*" >> SCRATCH.md
      - name: Check for changes
        id: changes
        run: |
          if [ ! -f SCRATCH.md ] || ! git diff --quiet SCRATCH.md || git ls-files --others --exclude-standard | grep -q "^SCRATCH.md$"; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi
      - name: Commit and push changes
        if: steps.changes.outputs.changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --local user.email "noreply@anthropic.com"
          git config --local user.name "Claude"
          git add SCRATCH.md
          git commit -m "docs: auto summary (${{ steps.commit.outputs.sha }})"
          git pull --rebase
          git push
